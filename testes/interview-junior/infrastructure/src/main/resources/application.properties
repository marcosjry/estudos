spring.application.name=Brainweb Interview

## Server properties
server.compression.enabled=true
server.http2.enabled=true
server.compression.mime-types=application/json
spring.jackson.serialization.write_dates_as_timestamps=true
spring.jackson.mapper.propagate_transient_marker=true
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.default-property-inclusion=non_null

## JDBC
spring.flyway.enabled=true
spring.flyway.schemas=${SCHEMA_DB}
jdbc.url=jdbc:postgresql://db:5432/${POSTGRES_DB}
jdbc.username=${POSTGRES_USERNAME}
jdbc.password=${POSTGRES_PASSWORD}
jdbc.schema=${SCHEMA_DB}
